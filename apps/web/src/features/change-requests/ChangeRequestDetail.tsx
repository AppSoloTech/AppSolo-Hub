import { useQuery } from '@tanstack/react-query'; import { Link, useParams } from 'react-router-dom'; import { requestsApi } from '../../api.js';
export function ChangeRequestDetail() { const { changeRequestId = '' } = useParams(); const query = useQuery({ queryKey: ['change-request', changeRequestId], queryFn: () => requestsApi.detail(changeRequestId) }); if (query.isPending) return <p>Loading request…</p>; if (query.isError) return <section className="card"><h1>Request unavailable</h1><p className="error">This request was not found or you do not have access to it.</p><Link to="/">Back to requests</Link></section>; const request = query.data.data; return <><Link to={`/projects/${request.projectId}/change-requests`}>← All change requests</Link><article className="card" style={{ marginTop: '20px' }}><p className="eyebrow">{request.status.replaceAll('_', ' ')} · {request.priority} priority</p><h1>{request.title}</h1><p>{request.description}</p><dl><dt>Requested completion</dt><dd>{request.requestedCompletionDate ?? 'Not specified'}</dd><dt>Submitted</dt><dd>{new Date(request.createdAt).toLocaleString()}</dd></dl></article></>; }
