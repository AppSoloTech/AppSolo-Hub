import { useQuery } from '@tanstack/react-query'; import { Link, useParams } from 'react-router-dom'; import { requestsApi } from '../../api.js'; import styles from './ChangeRequests.module.css';
export function ChangeRequestList() { const { projectId = '' } = useParams(); const query = useQuery({ queryKey: ['change-requests', projectId], queryFn: () => requestsApi.list(projectId) });
  if (query.isPending) return <p aria-live="polite">Loading change requests…</p>;
  if (query.isError) return <section className="card"><p className="error">{query.error instanceof Error && query.error.message === 'You do not have access to this project.' ? 'You do not have access to this project.' : 'We could not load change requests.'}</p><button className="button" onClick={() => void query.refetch()}>Try again</button></section>;
  return <><header className="pageHeader"><div><p className="eyebrow">Northstar client portal</p><h1>Change requests</h1><p>Track requests, priorities, and the next decision.</p></div><Link className="button" to={`/projects/${projectId}/change-requests/new`}>New request</Link></header>{query.data.data.length === 0 ? <section className="card"><h2>No change requests yet</h2><p>Start the shared record with the first request for this project.</p></section> : <section className={styles.list}>{query.data.data.map((item) => <Link className={styles.row} key={item.id} to={`/change-requests/${item.id}`}><div><span className={`${styles.status} ${styles[item.status]}`}>{item.status.replaceAll('_', ' ')}</span><h2>{item.title}</h2><p>{item.description}</p></div><div className={styles.meta}><strong>{item.priority}</strong><time>{new Date(item.createdAt).toLocaleDateString()}</time></div></Link>)}</section>}</>;
}
